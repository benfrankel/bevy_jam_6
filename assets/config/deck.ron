(
    initial_player_deck: PlayerDeck(
        health: 50,
        storage: [
            Module(condition: Nothing, effect: Missile),
            Module(condition: Nothing, effect: Heal),
            Module(condition: Missile, effect: Missile),
            Module(condition: Missile, effect: Heal),
            Module(condition: Heal, effect: Missile),
            Module(condition: Heal, effect: Heal),
        ],
        reactor: [
            Module(status: SlotEmpty),
            Module(status: SlotEmpty),
            Module(status: SlotEmpty),
        ],
    ),
)
